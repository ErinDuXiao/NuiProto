# 引き継ぎ

- 最終更新: 2026-09-04
- この文書は、セッションをまたいで作業を再開するためのもの

---

## いまどこにいるか

| | 状態 |
|---|---|
| 第1フェーズ（MVP） | **完成**。依頼書28章の完成条件14項目を実機で確認済み |
| 第2フェーズ（同居） | **設計・実装計画が承認済み。実装は未着手** |

**次にやること**: [実装計画](superpowers/plans/2026-09-04-companionship.md) の
**Task 1 Step 0**（ログイベント型の前倒し追加）から。

## 実測値（2026-09-04 時点）

```
テスト   257 件 / 16 ファイル  すべて通過
型検査   エラー 0（tsc --noEmit、any なし）
ビルド   274.41 kB (gzip 90.78 kB) + CSS 5.07 kB
```

作業ツリーはクリーン。未コミットの変更なし。

## リポジトリの状態（要注意）

- 作業ブランチは **`feat/plush-crane-mvp`**
- **既定ブランチは `main` ではなく `master`**
- `master` は 4 コミットで止まっており、**実装 19 コミットはブランチ側のみ・未マージ**

### 判断を仰ぎたいこと

`feat/plush-crane-mvp` を `master` にマージするかどうか。
指示がなかったため触っていません。実装再開の前に決めてください。

```bash
git log --oneline master..feat/plush-crane-mvp   # 何が未マージか見る
```

---

## 読む順番

はじめて触る場合、または久しぶりに戻る場合はこの順で。

1. [README.md](../README.md) — 起動方法、遊び方、Developer Menu の開き方
2. [実プレイ評価レポート](playtest-report.md) — **何が難しかったかが全部書いてある**
3. [MVP設計書](superpowers/specs/2026-09-03-plush-crane-mvp-design.md) — なぜその設計にしたか
4. [同居フェーズ設計書](superpowers/specs/2026-09-04-companionship-design.md) — 次にやること
5. [同居フェーズ実装計画](superpowers/plans/2026-09-04-companionship.md) — タスク分解

コードから読み始めないでください。このプロジェクトは
「なぜそうしたか」がコードより文書に多く書かれています。

---

## このプロジェクトの芯

見失いやすいので最初に書きます。

> クレーンは**入手方法**。棚は**生活する場所**。
> ぬいぐるみ同士の関係が**感情的報酬**。

プレイヤーが「この子を取った」ではなく「**この子がうちに来た**」、
さらに「**この子たち、一緒に暮らしてる**」と感じることを目指しています。

第1フェーズの実プレイで分かった最も重要なこと:

- 景品を落とした瞬間、**プレイヤーは景品ではなく見守りぬいぐるみを見た**
- 2匹が並ぶ嬉しさの中身は「並んだこと」ではなく、**先輩が振り向いて見たこと**だった

どちらも「モノが増えた」ではなく「関係が生まれた」ことによります。
第2フェーズはこの関係を棚でも持続させるのが目的です。

---

## 作業のしかた（ユーザーの明示指示）

**各段階の終わりに必ず Codex レビューを回すこと。**
2026-09-03 に「各段階で、codex reviewを入れてください」と指示を受けています。

```
/codex:rescue <レビューしてほしい対象と観点>
```

段階の切り方（実績）: 仕様確定時 → 実装計画確定時 → 各実装フェーズ完了時 → 全体完成時。

背景実行にして、待つ間に次の作業を進めてください。
指摘は必ずトリアージし、**仕様やユーザーの明示要件と衝突するものは理由を述べて却下**、
却下したことをコミットメッセージに残してください。

### Codex レビューで実際に見つかったもの

やる価値があることの証拠として残しておきます。いずれも自力では気づけていませんでした。

| 段階 | 指摘 |
|---|---|
| MVP仕様 | 難易度の数式が保証になっていない（個体差5倍、最悪個体は4回目でも取れない） |
| MVP計画 | 解析テストが実物を測っていない。`enforceAdvance` が保証になっていない |
| MVP実装 | 最終配置が**狙っていない景品を出口に押し込みうる**（理由の分からない獲得） |
| MVP完成 | **私のレポート自体に事実誤認2件**（保証の条件、保存済みと書いた未実装フィールド） |
| 同居仕様 | 「所持上限12」「リンク最大20」がどちらも事実誤認 |
| 同居計画 | **テストの前提となる距離計算が1回ずれている**。ID文字列から型を復元する設計の脆さ |

---

## 再開時に踏んでほしくない地雷

計画書に理由付きで書いてありますが、**一度間違えて直した箇所**なので念のため。

### 1. `Body.defId` と `CraneBoardSave.prizes[].defId` を改名しない

第2フェーズで `OwnedPlush.defId` → `PlushInstance.plushTypeId` に改名しますが、
**アーケード側の `defId` は別物**です。全域置換すると物理・盤面保存・`getPlush()` が
すべて壊れます。型名で絞って置換してください。

### 2. ID 文字列から種類を復元しない

`bodyId.split("#")[0]` で種類を取り出す設計は、ID の作り方を変えた瞬間に
**静かに壊れます**。物理が景品を取り除くときに種類も一緒に報告する形にします
（計画 Task 2）。

### 3. 隣接を距離だけで決めない

12匹を同じ場所に重ねると総当たりで66本のリンクが張られます。それは「隣にいる」
ではありません。左右上下でいちばん近い1匹だけとリンクします（計画 Task 4）。

### 4. 空中の物体に「床を転がる距離」の初速を与えない

第1フェーズで**掴み失敗が獲得に化けていた**原因です。摩擦は接地中しか効かないため、
30px のつもりが 130px 以上飛んでいました。`advanceImpulse` のコメントに
警告を書いてあります。

### 5. 「4回以内に取れる」は無条件の保証ではない

**同じ景品を継続して狙った場合**に限ります。不変条件は「その試行の対象」に
対して働くため、毎回別の景品を狙えば1匹あたりの前進は遅くなります。
コード・テスト・レポートのすべてにこの条件を併記してください。

---

## テストについての教訓

第1フェーズで、解析テストは通っていたのに実際の獲得率が
**想定 23.6% に対し実測 86.7%** でした。

> **数式や閾値だけを検証するテストを保証として扱わない。**
> 必ず実際のゲームフローを通したテストを入れる。

同じ理由で、次のテストは書いても意味がありません。

- `store.winPlush({attemptsToAcquire: 3})` を呼んで 3 が保存されることを確かめる
  → 「渡した値が保存される」ことしか言っていない
- `requiredHold(n) === RELEASE_THRESHOLD + drain(n)` を確かめる
  → 実装の写経

代わりに、実物の物理と状態機械を回して結果を見てください。

---

## 開発中に知っておくと楽なこと

### ブラウザでの動作確認

ブラウザペインが非表示のときは `requestAnimationFrame` が止まります
（アプリ側の `visibilitychange` ハンドラで意図的に止めている）。
そのため JS で待っても物理が進みません。**スクリーンショットを撮ると1フレーム進みます。**
連続で確認したいときはスクリーンショットを並べてください。

### Vite の変換キャッシュ

エクスポート名を変えたのに `ReferenceError: XXX is not defined` が出る場合、
HMR が古いモジュールを掴んでいます。

```bash
rm -rf node_modules/.vite
```

でキャッシュを消して dev サーバーを再起動してください。

### Developer Menu

棚画面の**右下の小さなドットを3回タップ**、または `Ctrl + Shift + D`。
所持品リセット、任意のぬいぐるみ追加、プレイログの JSON ダウンロードができます。

---

## 未解決事項（体験に影響しないため放置しているもの）

- `react-dom/server` をブラウザ側で使っており、バンドルにサーバー描画コードが
  約 40KB 入っている。`PlushSVG` を棚・クレーン・シェアで共有するための代償
- 厳しい CSP 下では `blob:` を `img-src` に許可しないとシェア画像が作れない
- 開発モードでは React StrictMode によりログが二重に記録される（本番では起きない）
- localStorage に**書けない**環境では、プレイはできるがリロードで失われる。
  棚の画面に静かな一行で伝えている
